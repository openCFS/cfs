# remove directory before every run
rm -rf precice-run

# remove old log files before every run
rm -f log.foam log.cfs

# use to check precice runs
# ls -la precice-run

# at this point openFOAM/preCICE must be closed via CTRL-C