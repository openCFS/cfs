SET(PHIST_SRCS
  PhistEigenSolver.cc
)

FIND_PACKAGE(phist REQUIRED CONFIG)

INCLUDE_DIRECTORIES(${PHIST_INCLUDE_DIRS})
ADD_LIBRARY(phist-olas STATIC ${PHIST_SRCS})
TARGET_LINK_LIBRARIES(phist-olas ${PHIST_LIBRARIES})

