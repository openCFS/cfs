SET(PHIST_SRCS PhistEigenSolver.cc)

ADD_LIBRARY(phist-olas STATIC ${PHIST_SRCS})

SET(TARGET_LL
  ${PHIST_LIBRARY} 
  ${GHOST_LIBRARY}
  ${HWLOC_LIBRARY} )

TARGET_LINK_LIBRARIES(phist-olas ${TARGET_LL})
