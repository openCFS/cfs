#include "DummyPreciceAdapter.hh"

// The DummyPreciceAdapter is header-only (all no-op inline methods); this
// translation unit only exists so the preciceadapter target always has the
// class in its object code.

namespace CoupledField
{
}
