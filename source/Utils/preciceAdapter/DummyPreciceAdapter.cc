
#include "DummyPreciceAdapter.hh"


namespace CoupledField
{
    // Force an out‐of‐line symbol so that object code is generated.
    // The __attribute__((used)) attribute prevents the compiler from optimizing this away.
// #if defined(__GNUC__)
//     __attribute__((used))
// #endif
    // void DummyPreciceAdapter::DummyPreciceAdapter_ForceBuild() {
    //     // Use a volatile variable to prevent optimization.
    //     volatile int dummy = 0;
    //     dummy++;
    // }
}
