SET(SIMINMPHTXT_SRCS SimInputMPHTXT.cc)

INCLUDE_DIRECTORIES(${MINIZIP_INCLUDE_DIR})

ADD_LIBRARY(siminmphtxt STATIC ${SIMINMPHTXT_SRCS})

TARGET_LINK_LIBRARIES(siminmphtxt
  ${MINIZIP_LIBRARY}
  ${ZLIB_LIBRARY}
  )