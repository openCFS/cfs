SET(SIMINENSIGHT_SRCS
  SimInputEnsight.cc
  ../SimInputVTKBased.cc
)

ADD_LIBRARY(siminensight STATIC ${SIMINENSIGHT_SRCS})

SET(TARGET_LL
  ${TARGET_LL}
  ${VTK_LIBRARY}
)

IF(DEBUG)
SET(TARGET_LL
  ${TARGET_LL}
  ${BOOST_REGEX_LIB_DEBUG}
)
ELSE(DEBUG)
SET(TARGET_LL
  ${TARGET_LL}
  ${BOOST_REGEX_LIB_RELEASE}
)
ENDIF(DEBUG)

TARGET_LINK_LIBRARIES(siminensight
  ${TARGET_LL}
  mesh
)
