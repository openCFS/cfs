SET(SIMINCGNS_SRCS
  SimInputCGNS.cc
)

LIST(APPEND TARGET_LL
  ${CGNS_LIBRARY}
  ${HDF5_LIBRARY}
  ${ZLIB_LIBRARY}
  )

ADD_LIBRARY(simincgns STATIC ${SIMINCGNS_SRCS})

TARGET_LINK_LIBRARIES(simincgns
  ${TARGET_LL}
  )

