INCLUDE_DIRECTORIES(${CGNS25_INCLUDE_DIR})

SET(SIMINOUTCGNS_SRCS
  SimInputCGNS.cc
  SimOutputCGNS.cc
)

LIST(APPEND TARGET_LL
  ${CGNS25_SHARED_LIBRARY}
  ${HDF5_SHARED_LIBRARY}
  ${ZLIB_SHARED_LIBRARY}
  )

ADD_LIBRARY(siminoutcgns STATIC ${SIMINOUTCGNS_SRCS})

TARGET_LINK_LIBRARIES(siminoutcgns
  ${TARGET_LL}
  )

