SET(LOGGING_SRCS LogConfigurator.cc)

SET(TARGET_LL
  paramh
  utils
  ${BOOST_LIBRARY} 
  ${ZLIB_LIBRARY} )

ADD_LIBRARY(logging STATIC ${LOGGING_SRCS})

ADD_DEPENDENCIES(logging boost)

TARGET_LINK_LIBRARIES(logging ${TARGET_LL} ${BOOST_LIBRARY} ${ZLIB_LIBRARY} )
