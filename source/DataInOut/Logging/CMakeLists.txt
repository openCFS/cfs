SET(LOGGING_SRCS LogConfigurator.cc)

SET(TARGET_LL
  paramh
  utils
  ${BOOST_FILESYSTEM_LIB}
  ${BOOST_THREAD_LIB}
  )

ADD_LIBRARY(logging STATIC ${LOGGING_SRCS})

ADD_DEPENDENCIES(logging boost)

TARGET_LINK_LIBRARIES(logging
  ${TARGET_LL}
  ${BOOST_LOG_LIB}
  )
