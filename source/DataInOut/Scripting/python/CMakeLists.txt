INCLUDE_DIRECTORIES(${PYTHON_INCLUDE_PATH})

SET(PYTHONMESS_SRCS py-messenger.cc)

ADD_LIBRARY(pythonmess STATIC ${PYTHONMESS_SRCS})

TARGET_LINK_LIBRARIES(pythonmess ${BOOST_PYTHON_LIB} ${PYTHON_LIBRARY})