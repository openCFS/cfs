IF(USE_XERCES)
  INCLUDE_DIRECTORIES(${XERCES_INCLUDE_DIR})
ENDIF(USE_XERCES)

IF(CFS_BLAS_LAPACK STREQUAL "MKL")
  SET_SOURCE_FILES_PROPERTIES(programOptions.cc
    PROPERTIES COMPILE_FLAGS "-I${MKL_INCLUDE_DIR}")
ENDIF(CFS_BLAS_LAPACK STREQUAL "MKL")

SET(DATAINOUT_SRCS 
  coloredConsole.cc
  freqfunc.cc
  postProc.cc
  programOptions.cc
  ResultCache.cc
  resultHandler.cc
  simOutput.cc
  WriteInfo.cc
  XMLMaterialHandler.cc
)

ADD_LIBRARY(datainout STATIC ${DATAINOUT_SRCS})

SET(TARGET_LL
  materials
  simouttext
  ${BOOST_PROGRAM_OPTIONS_LIB}
  ${BOOST_FILESYSTEM_LIB}
  ${BOOST_SYSTEM_LIB}
)

IF(USE_XERCES)
  SET(TARGET_LL
    ${TARGET_LL}
    ${XERCES_LIBRARY}
    )
ENDIF(USE_XERCES)

TARGET_LINK_LIBRARIES(datainout ${TARGET_LL})
