SET(PARAMH_SRCS
  XMLSkeletonConf.cc
  ParamNode.cc)

IF(USE_XERCES)

  INCLUDE_DIRECTORIES(${XERCES_INCLUDE_DIR})

  SET(PARAMH_SRCS
    ${PARAMH_SRCS}
    Xerces.cc)

ENDIF(USE_XERCES)

ADD_LIBRARY(paramh STATIC ${PARAMH_SRCS})

IF(USE_XERCES)
  SET(TARGET_LL ${XERCES_LIBRARY})
ENDIF(USE_XERCES)

SET(TARGET_LL
  ${TARGET_LL}
  ${BOOST_FILESYSTEM_LIB}
  ${BOOST_SYSTEM_LIB}
  )

TARGET_LINK_LIBRARIES(paramh utils ${TARGET_LL})
