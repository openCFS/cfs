SET(PARAMH_SRCS
  XMLSkeletonConf.cc
  ParamNode.cc
  XMLMaterialHandler.cc
)


INCLUDE_DIRECTORIES(${XERCES_INCLUDE_DIR})

SET(PARAMH_SRCS
  ${PARAMH_SRCS}
  Xerces.cc)

ADD_LIBRARY(paramh STATIC ${PARAMH_SRCS})

SET(TARGET_LL
  materials
  ${BOOST_FILESYSTEM_LIB}
  ${BOOST_SYSTEM_LIB}
  )

LIST(APPEND TARGET_LL ${XERCES_LIBRARY})


TARGET_LINK_LIBRARIES(paramh ${TARGET_LL})
