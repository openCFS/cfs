// Define used to check if Ensight reader is used

#cmakedefine USE_ENSIGHT

// a C++ define VTK_VERSION conflicts with the original define from the vtk headers.
#define CFS_VTK_VERSION "@VTK_VERSION@"