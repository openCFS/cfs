The Ilupack-OpenMP version is built using the provided built scripts called from our cmake.
There are some modifications to the COMPILE_ALL.sh for the ilupack to use same comilers as set in CFS.
There is also COPY_LIB.sh added to the source directory which should can be moved to cmake in future.