--- CMakeLists.txt.orig	2012-01-17 03:51:23.000000000 +0100
+++ CMakeLists.txt	2012-04-13 22:54:40.000000000 +0200
@@ -165,9 +165,9 @@
 
 if(NOT SKIP_INSTALL_LIBRARIES AND NOT SKIP_INSTALL_ALL )
     install(TARGETS zlib
-        RUNTIME DESTINATION bin
-        ARCHIVE DESTINATION lib
-        LIBRARY DESTINATION lib )
+        RUNTIME DESTINATION bin/${ARCH_STR}
+        ARCHIVE DESTINATION ${LIB_SUFFIX}/${ARCH_STR}
+        LIBRARY DESTINATION ${LIB_SUFFIX}/${ARCH_STR} )
 endif()
 if(NOT SKIP_INSTALL_HEADERS AND NOT SKIP_INSTALL_ALL )
     install(FILES ${ZLIB_PUBLIC_HDRS} DESTINATION include)
