--- ThirdParty/zlib/vtkzlib/zutil.h.orig
+++ ThirdParty/zlib/vtkzlib/zutil.h
@@ -118,7 +118,8 @@
 #  endif
 #endif

-#if defined(MACOS) || defined(TARGET_OS_MAC)
+/* Modern macOS (__APPLE__) is Unix-based and has fdopen - skip this block */
+#if (defined(MACOS) || defined(TARGET_OS_MAC)) && !defined(__APPLE__)
 #  define OS_CODE  0x07
 #  ifndef Z_SOLO
 #    if defined(__MWERKS__) && __dest_os != __be_os && __dest_os != __win32_os
